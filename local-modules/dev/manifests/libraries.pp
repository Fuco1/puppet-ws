class libraries {
  package { "libboost1.54-all-dev": ensure => installed }
  package { "libreadline-dev": ensure => installed }
  package { "libxml2-dev": ensure => installed }
  package { "libpng12-dev": ensure => installed }
  package { "libjpeg-turbo8-dev": ensure => installed }
  package { "libsvga1-dev": ensure => installed }
  package { "libtiff4-dev": ensure => installed }
  package { "libgif-dev": ensure => installed }
  package { "libotf-dev": ensure => installed }
  package { "libgtk-3-dev": ensure => installed }
  package { "libxpm-dev": ensure => installed }
  package { "libncurses5-dev": ensure => installed }
  package { "libasound2-dev": ensure => installed }
  package { "libncursesw5-dev": ensure => installed }
  package { "libglib2.0-dev": ensure => installed }
  package { "libpulse-dev": ensure => installed }
  package { "libid3tag0-dev": ensure => installed }
  package { "libogg-dev": ensure => installed }
  package { "libsamplerate0-dev": ensure => installed }
  package { "libmad0-dev": ensure => installed }
  package { "libmikmod2-dev": ensure => installed }
  package { "libmodplug-dev": ensure => installed }
  package { "libresid-builder-dev": ensure => installed }
  package { "libsidplay2-dev": ensure => installed }
  package { "libwavpack-dev": ensure => installed }
  package { "libmpcdec-dev": ensure => installed }
  package { "libaudiofile-dev": ensure => installed }
  package { "libavahi-core-dev": ensure => installed }
  package { "libavahi-client-dev": ensure => installed }
  package { "libavahi-glib-dev": ensure => installed }
  package { "libflac-dev": ensure => installed }
  package { "libbz2-dev": ensure => installed }
  package { "libiso9660-dev": ensure => installed }
  package { "libzzip-dev": ensure => installed }
  package { "libmms-dev": ensure => installed }
  package { "libao-dev": ensure => installed }
  package { "libavcodec-dev": ensure => installed }
  package { "libavformat-dev": ensure => installed }
  package { "libmp3lame-dev": ensure => installed }
  package { "libtag1-dev": ensure => installed }
  package { "libdjvulibre-dev": ensure => installed }
  package { "libmagic-dev": ensure => installed }
  package { "libcrypto++-dev": ensure => installed }
  package { "libspectre-dev": ensure => installed }
  package { "libarchive-dev": ensure => installed }
  package { "libfreetype6-dev": ensure => installed }
  package { "libjbig2dec0-dev": ensure => installed }
  package { "libjpeg-dev": ensure => installed }
  package { "libcurl4-openssl-dev": ensure => installed }
  package { "libwrap0-dev": ensure => installed }
  package { "libsqlite3-dev": ensure => installed }
  package { "libyajl-dev": ensure => installed }
  package { "libcdio-dev": ensure => installed }
  package { "libadplug-dev": ensure => installed }
  package { "libfaad-dev": ensure => installed }
  package { "libgme-dev": ensure => installed }
  package { "libshout3-dev": ensure => installed }
  package { "libmpg123-dev": ensure => installed }
  package { "libopus-dev": ensure => installed }
  package { "libsndfile1-dev": ensure => installed }
  package { "libvorbis-dev": ensure => installed }
  package { "libwildmidi-dev": ensure => installed }
  package { "libtwolame-dev": ensure => installed }
  package { "libroar-dev": ensure => installed }
  package { "libopenal-dev": ensure => installed }
  package { "libevent-dev": ensure => installed }
  package { "libsoup2.4-dev": ensure => installed }
  package { "libwebkit-dev": ensure => installed }
  package { "libwebkitgtk-3.0-dev": ensure => installed }
  package { "libmagickcore-dev": ensure => installed }
  package { "libmagick++-dev": ensure => installed }
}
