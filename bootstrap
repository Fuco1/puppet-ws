# -*- mode: shell-script -*-
cd $HOME
apt-get update
apt-get install -y git puppet librarian-puppet
git clone https://github.com/Fuco1/puppet-ws.git
# remove bootstrap git, we will install latest from ppa
apt-get remove -y git
cd puppet-ws
./run
